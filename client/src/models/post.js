class Post {
    constructor(id,title, body, category_id) {
        this.id=id;
        this.title = title;
        this.body = body;
        this.category_id = category_id;
    }
}

export default Post;